namespace Varguiniano.YAPU.Runtime.Player
{
    /// <summary>
    /// Enumeration of the difficulties catching has.
    /// </summary>
    public enum CatchDifficulty
    {
        Guaranteed,
        Easy,
        Normal,
        Hard
    }
}